-- Optional seed data for local development.
-- Run with: psql -h localhost -U postgres -d buildora -f infra/docker/seed.sql

-- Example lead stub; adjust as needed.
-- INSERT INTO "Lead" (id, source, status, intentScore, createdAt, updatedAt)
-- VALUES (gen_random_uuid(), 'import', 'new', 0, NOW(), NOW());
